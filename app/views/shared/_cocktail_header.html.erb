<div class="cocktail-banner p-4">
  <h1 class="mt-4"><%= @cocktail.name %></h1>
  <div class="card mt-3">
    <div class="card-body">
      <div class="card">
      <h5 class="card-header">Ingredients</h5>
        <div class="card-body">
          <% @cocktail.doses.each do |dose| %>
        <div class="ingredient-label "> • <%= dose.description %> <%= dose.ingredient.name %> </div>
      <% end %>
        </div>
      </div>
      <h5 class="card-title mt-3">Description</h5>

      <p class="card-text"> <%= @cocktail.description %> </p>
      <span>
        <i class="far fa-edit" style="color: #ff7d79;"></i> Edit
         | <i class="fas fa-backspace" style="color: #ff7d79;"></i> Delete</i>
       </span>
    </div>
  </div>
  <div class="mt-3">
    <%= link_to "Back to Cocktails", root_path %>
  </div>
</div>
